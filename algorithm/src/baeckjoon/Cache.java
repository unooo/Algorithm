package baeckjoon;

import java.util.HashMap;
import java.util.List;
import java.util.Locale.Category;
import java.util.Map;




public class Cache {
/*
	 private final Map<String, List<Item>> ItemCache = new HashMap<String, Listlt;Category>>();
	 
	 	private ItemDao itemDao;
	    // 질의 메서드
	    @Override
	    public List<Item> findItem(String ItemCode) {
	        // 데이터가 적재되지 않았으면 데이터 저장소(DB)에서 데이터 가져오기
	        if (ItemCache.isEmpty()) {
	            synchronized (ItemCache) {
	                if (ItemCache.isEmpty()) {
	                    Map<String, List<Item>> map = new HashMap<String, List<Item>>();
	                    List list = itemDao.findAllItems();
	                    for (Item c : list)
	                        map.put(c.ItemCache(), c);
	 
	                    ItemCache.clear();
	                    ItemCache.putAll(map);
	                }
	            }
	        }
	 
	        return ItemCache.get(ItemCode);
	    }
	    
	    static class Item{
	    	//Item VO...
	    }

	     class ItemDao{
	    	//Dao code...
	    	
	    }
	    */
}

